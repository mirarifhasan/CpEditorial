@model CpEditorial.Models.SignupModel
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Sign Up</title>
    <link href="~/Content/site.css" rel="stylesheet">

</head>
<body>
    @*NavBer*@
    @{
        Html.RenderPartial("NavigationBar");
    }

    @*Signup from section*@
    <section class="contact-area section-padding-100">
        <div class="container">
            <div class="row justify-content-center">
                <!-- Signup Form Area -->
                <div class="col-10 col-md-8 col-lg-6">
                    <div class="contact-form form-group">
                        <h4 style="text-align:center;">Sign Up</h4>
                        <!-- Signup Form -->
                        @using (Html.BeginForm("AddUser", "Register"))
                        {
                            @Html.AntiForgeryToken()
                            <div class="row">
                                @Html.ValidationSummary(true, "", new { @class = "text-denger"})
                                <div class="col-12 col-md-12">
                                    <div class="group">
                                        <label for="userName">User Name</label>
                                        @Html.TextBoxFor(m => m.userName, new { @class = "form-control", @required = true })
                                    </div>
                                </div>
                                <div class="col-12 col-md-12">
                                    <div class="group">
                                        <label for="email">Email</label>
                                        @Html.TextBoxFor(m => m.email, new { @class = "form-control", @required = true, @type = "email" })
                                    </div>
                                </div>
                                <div class="col-12 col-md-12">
                                    <div class="group">
                                        <label for="password">Password</label>
                                        @Html.TextBoxFor(m => m.password, new { @class = "form-control", @required = true, @type = "password" })
                                    </div>
                                </div>
                                <div class="col-12 col-md-12">
                                    <div class="group">
                                        <label for="confirmPassword">Confirm Password</label>
                                        @Html.TextBoxFor(m => m.confirmPassword, new { @class = "form-control", @required = true, @type = "password" })
                                    </div>
                                </div>

                                <div class="col-12" style="text-align:center;">
                                    <button type="submit" class="btn world-btn">Sign Up</button>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </section>


    @*Footer*@
    @{
        Html.RenderPartial("Footer");
    }


    @if (ViewBag.Error != null)
    {
        <script>
            alert("@ViewBag.Error");
        </script>
    }

</body>
</html>

